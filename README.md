# Go SDK

Go 1.26.5 for Cpak development environments.

The image exports `go` and `gofmt` for amd64 and arm64. The Go archive is checked against the official SHA-256 before it is copied into the final image.

```sh
cpak install github.com/containerpak/sdk-go
```
